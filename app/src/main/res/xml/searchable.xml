<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/search_hint"
    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer"
    android:voiceLanguageModel="web_search"
    android:voiceLanguage="@string/locale"
    android:includeInGlobalSearch="true"
    android:searchSettingsDescription="@string/search_setting_description"
    android:voicePromptText="@string/search_prompt_text"
    android:voiceMaxResults="@integer/MAX_VOICE_RESULTS"
    >
</searchable>